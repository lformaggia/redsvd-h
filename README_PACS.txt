This is a software for SVD decomposition (useful for principal component analysis)
To install it and have it available in the pacs directory hierarchy just type
make

It's a template-only library so you just install the header file
